% ---
% INICIO DAS CUSTOMIZACOES (ARQUIVO capa.sty)
% ---
\ProvidesPackage{capa}[2025/11/02 Customizacao para Doutorado em Engenharia Eletrica - UnB]

% Comandos para os novos campos
\newcommand{\linhadepesquisa}[1]{\gdef\@linhadepesquisa{#1}}
\newcommand{\areadeconcentracao}[1]{\gdef\@areadeconcentracao{#1}}
\newcommand{\imprimirlinhadepesquisa}{\@linhadepesquisa}
\newcommand{\imprimirareadeconcentracao}{\@areadeconcentracao}

\makeatletter % Necessário para usar \@areadeconcentracao etc.

% ---
% CAPA
% ---
\renewcommand{\imprimircapa}{%
  \begin{capa}%
  \begin{center}

  % Logo da universidade (se existir)
  \IfFileExists{imagens/unb_bandeira.png}{%
    \includegraphics[width=70mm]{imagens/unb_bandeira.png}\\[0.3cm]
  }{%
    % Se não houver logo, deixar espaço
    \vspace*{1cm}
  }

  % Instituicao (UnB, FT, PPGEE) - FONTE NORMAL DO DOCUMENTO
  {\Large\imprimirinstituicao}
      
  \vspace*{1.5cm}
      
  % Autor - FONTE NORMAL
  {\large\imprimirautor}

  \vspace*{\fill}

  % Titulo - FONTE NORMAL, NEGRITO
  \begin{center}
  {\bfseries\Large\MakeUppercase{\imprimirtitulo}}
  \end{center}

  \vspace*{\fill}

  % Preambulo (O texto do "Pre-projeto apresentado...")
  \abntex@ifnotempty{\imprimirpreambulo}{%
    \hspace{.45\textwidth}
    \begin{minipage}{.5\textwidth}
    \SingleSpacing
    \small
    \imprimirpreambulo
    \end{minipage}%
    \vspace*{0.5cm}
  }%

  % Novos campos: Area e Linha - CENTRALIZADOS
  \abntex@ifnotempty{\@areadeconcentracao}{%
      \begin{center}
      \SingleSpacing
      \small
      \textbf{Área de Concentração:} \imprimirareadeconcentracao
      
      \abntex@ifnotempty{\@linhadepesquisa}{%
          \\
          \textbf{Linha de Pesquisa:} \imprimirlinhadepesquisa
      }%
      \end{center}
      \vspace*{\fill}
  }%

  % Local e Data
  {\large\imprimirlocal}
  \par
  {\large\imprimirdata}
  \vspace*{1cm}

  \end{center}
  \end{capa}
  \newpage
}

% folha de rosto - DEIXAR VAZIA para não ter conflito
\renewcommand{\folhaderostocontent}{}

\makeatother

% ---
% FIM DAS CUSTOMIZACOES
% ---
